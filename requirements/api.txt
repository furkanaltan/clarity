-r base.txt

# Imported by rove_app_api.py. Production API-venv versions are UNKNOWN because
# the read-only SSH account cannot execute or inspect /root/rove-app-api-venv.
Flask
argon2-cffi
cryptography==45.0.7
openai
pywebpush
