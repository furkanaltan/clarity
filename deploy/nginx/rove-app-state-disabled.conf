# Include this location in the getrove.de server block during Security Fix 9.1 cutover.
# It prevents stale public state JSON files from being served even before cleanup completes.
location ^~ /app-state/ {
    return 410;
}
